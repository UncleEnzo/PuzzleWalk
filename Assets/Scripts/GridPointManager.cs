using UnityEngine;
using System.Collections;

public class GridPointManager : MonoBehaviour {

	//Finds grid points and place them in an array
	//Each object in the array knows the other four points around it
	//Start by manually filling them

	public GameObject[] nearbyPoints;
	public bool movementSpace = true;

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
